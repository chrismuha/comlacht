# Original source archive

These files preserve the original pre-redesign source code verbatim, including paths relative to their former locations. They are intentionally not imported by the live application, so their styles and placeholder components cannot affect the deployed website.

The working application remains in `src/App.vue`, `src/Views/HomeView.vue`, `src/main.js`, and `src/assets/main.css`.
